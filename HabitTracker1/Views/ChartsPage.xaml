<Page x:Class="HabitTracker1.Views.ChartsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Графики"
      Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2" 
                   Text="Графики и статистика" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <!-- Линейный график (placeholder) -->
        <Border Grid.Row="1" Grid.Column="0" 
                Background="White" 
                Margin="0,0,10,0" 
                Padding="20" 
                CornerRadius="5"
                BorderBrush="#BDC3C7"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Прогресс привычки" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,20"/>
                <Canvas Background="#ECF0F1" Height="200">
                    <!-- Здесь будет график -->
                </Canvas>
            </StackPanel>
        </Border>

        <!-- Круговая диаграмма (placeholder) -->
        <Border Grid.Row="1" Grid.Column="1" 
                Background="White" 
                Margin="10,0,0,0" 
                Padding="20" 
                CornerRadius="5"
                BorderBrush="#BDC3C7"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Распределение по категориям" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,20"/>
                <Canvas Background="#ECF0F1" Height="200">
                    <!-- Здесь будет диаграмма -->
                </Canvas>
            </StackPanel>
        </Border>

        <!-- Тепловая карта -->
        <Border Grid.Row="2" Grid.ColumnSpan="2" 
                Background="White" 
                Margin="0,20,0,0" 
                Padding="20" 
                CornerRadius="5"
                BorderBrush="#BDC3C7"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Тепловая карта (месяц)" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,20"/>
                <ItemsControl>
                    <!-- Здесь будет тепловая карта -->
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</Page>
