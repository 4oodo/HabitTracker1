<Page x:Class="HabitTracker1.Views.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Статистика"
      Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Статистика привычек" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <!-- Выбор периода -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="PeriodComboBox" Width="150" Padding="10,5" Background="White" SelectionChanged="PeriodComboBox_SelectionChanged">
                <ComboBoxItem Content="День" Tag="1"/>
                <ComboBoxItem Content="Неделя" Tag="7" IsSelected="True"/>
                <ComboBoxItem Content="Месяц" Tag="30"/>
                <ComboBoxItem Content="Год" Tag="365"/>
            </ComboBox>
        </StackPanel>

        <!-- Статистика по привычкам -->
        <ListBox Grid.Row="2" x:Name="StatisticsListBox" Background="#F5F5F5" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" 
                            Margin="0,0,0,10" 
                            Padding="20" 
                            CornerRadius="5"
                            BorderBrush="#BDC3C7"
                            BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Column="0" 
                                       Text="{Binding Name}" 
                                       FontSize="14" 
                                       FontWeight="Bold"
                                       Foreground="#2C3E50"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                       Text="{Binding SuccessPercentage, StringFormat='Успешность: {0:F1}%'}" 
                                       FontSize="12"
                                       Foreground="#7F8C8D"
                                       Margin="0,10,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" 
                                       Text="{Binding CurrentStreak, StringFormat='Серия: {0} дней'}" 
                                       FontSize="12"
                                       Foreground="#7F8C8D"
                                       Margin="0,3,0,0"/>

                            <!-- Прогресс-бар -->
                            <ProgressBar Grid.Column="1" 
                                         Grid.Row="0" 
                                         Grid.RowSpan="3"
                                         Value="{Binding SuccessPercentage}" 
                                         Maximum="100"
                                         Height="30"
                                         Margin="20,0,0,0"
                                         Background="#ECF0F1"
                                         Foreground="#27AE60"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
